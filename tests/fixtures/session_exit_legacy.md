reason: GRACEFUL_CHECKPOINT
session_id: D-T200

Legacy session_exit.md format — only the freeform markdown body, no JSON block.
post_director.py should detect this and route to legacy_finalize() (Stage R3 / Task 10).
